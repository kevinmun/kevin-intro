---
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
---

<section class="hero">
  <p class="greeting">Hi, I'm</p>
  <h1>Kevin Mun</h1>
  <p class="tagline">Head of Engineering &amp; Senior Mobile Engineer building products across iOS, Android, and the web.</p>
  <div class="cta">
    <a href="#about">About me</a>
    <a href="https://github.com/kevinmun" class="secondary" target="_blank" rel="noopener">GitHub &rarr;</a>
    <a href={`${base}articles/`} class="secondary">Articles &rarr;</a>
  </div>
</section>

<style>
  .hero {
    padding-block: var(--space-2xl);
    text-align: center;
  }

  .greeting {
    font-size: 1rem;
    color: var(--color-text-muted);
    margin-bottom: 0;
  }

  h1 {
    font-size: 2.75rem;
    letter-spacing: -0.02em;
    margin-top: var(--space-xs);
  }

  .tagline {
    font-size: 1.15rem;
    color: var(--color-text-muted);
    max-width: 480px;
    margin-inline: auto;
  }

  .cta {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    margin-top: var(--space-lg);
  }

  .cta a {
    padding: 0.6rem 1.4rem;
    border-radius: var(--radius);
    font-size: 0.95rem;
    font-weight: 500;
    background: var(--color-accent);
    color: #fff;
    transition: background 0.15s ease;
  }

  .cta a:hover {
    background: var(--color-accent-hover);
    text-decoration: none;
  }

  .cta .secondary {
    background: transparent;
    color: var(--color-accent);
    border: 1px solid var(--color-border);
  }

  .cta .secondary:hover {
    border-color: var(--color-accent);
    background: transparent;
  }

  @media (max-width: 640px) {
    h1 {
      font-size: 2.25rem;
    }

    .cta {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
